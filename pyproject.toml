[project]
name = "koder"
version = "0.1.0"
description = "CLI-based code agent powered by LangGraph inspired by Anthropic Claude Code"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
